{
  "name": "lexguard-workspace-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "node runner.js --dev",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "start": "node runner.js"
  }
}
